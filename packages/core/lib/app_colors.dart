import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const Color primary = Color(0xFF083B6E);
  static const Color primaryLight = Color(0xFF0C4A84);
  static const Color primaryMuted = Color(0xFFAFC3D7);

  // Background
  static const Color background = Color(0xFFF3F5F8);
  static const Color surface = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF083B6E);
  static const Color textSecondary = Color(0xFF4B5563);
  static const Color textMuted = Color(0xFF6B7280);
  static const Color textDisabled = Color(0xFF9CA3AF);

  // Border
  static const Color border = Color(0xFFD9DEE5);
  static const Color borderLight = Color(0xFFE5E7EB);

  // Status
  static const Color success = Color(0xFF027A48);
  static const Color successLight = Color(0xFFE8FFF3);
  static const Color danger = Color(0xFFD92D20);
  static const Color dangerLight = Color(0xFFFFECEB);

  // Filter
  static const Color filterActive = Color(0xFF6E97CC);
  static const Color filterBackground = Color(0xFFE9EDF2);
}